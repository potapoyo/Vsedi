# 2026-08-12

## 09:33 — M2結果を確認し、M3計画とIssueを準備

### 指示

- 最新の`main`をpullする
- M2の結果を確認する
- M3の計画を作成する
- M3のIssueを発行する
- 日記を含む変更をコミットし、M3実装を開始できる状態にする

### M2確認

- `main`を`origin/main`へfast-forwardし、先端commit `5a4ab14`を取得した
- M2のnative GUI確認結果を読み、Unity / VRChat / VPM診断、Git境界診断、VPM方針保持、非対応project、読取警告、最近のproject、log window、log export / sanitizationが合格済みであることを確認した
- GUI確認後に残っていたmacOSのlog folder起動とGit root path表示は、先端commitで修正されていることを確認した
- `mise exec -- cargo test --manifest-path src-tauri/Cargo.toml`が成功した（21 passed, 0 failed）
- `mise exec -- cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`が成功した
- `pnpm build`と`mise exec -- pnpm check-generated-types`が成功した
- 先端commitで修正されたFinderによるlog directory表示とGit root path非表示は、M3の状態変更機能へ着手する前の実機再確認項目として残した

### M3計画

- `docs/development/m3-plan.md`を追加し、M3の目的、対象範囲、安全方針、共有型、GitAdapter方針、実装フェーズ、test matrix、Internal Alpha完了条件を整理した
- 保存前previewと実際のcommit対象を一致させ、preview後に状態が変化した場合はcommitを停止する方針とした
- 親folderがGit rootの場合はrepository全体を保存対象とし、project外の変更も隠さずpreviewする方針とした
- 既存staged変更は自動でreset / unstageせず、初期版では保存を停止する方針とした
- add後にcommitが失敗した場合はworktreeを巻き戻さず、indexが変化した可能性を表示する方針とした
- `docs/development/roadmap.md`から詳細計画を参照できるようにした

### Issue

- open Issueを検索し、M3の重複Issueがないことを確認した
- #18 — M3: ローカル保存と履歴確認を実装する
- https://github.com/potapoyo/Vsedi/issues/18
- Issue本文へM2からの開始条件、対象範囲、安全条件、5段階の実装順序、両OSを含む完了条件を記載した

### 実装準備

- READMEの現在段階を、M2診断完了・M3ローカル保存着手準備中へ更新した
- M3実装用branch `codex/m3-local-save`を作成し、計画と記録を最初のcommitにまとめる
- pushとPull Request作成は別の指示まで行わない

## M3実装 — ローカル保存の縦切り

- `git status --porcelain=v2 -z` を解析する変更一覧と、Rust正本の共有型を追加した。変更・削除・rename・未管理・競合と空白・日本語pathをテストで扱う。
- 未初期化projectでは、`.gitignore` / `Packages/.gitignore` の不足ruleをpreviewする。既存内容と改行形式は保持し、不足ruleだけを追記する。
- preview tokenが変化していない場合だけ `git init`、または `git add -A` / `git commit` を実行する。既存staged変更・競合・空変更・空メモは保存しない。
- 保存成功時にはcommit ID、時刻、file数を表示し、履歴とcommit詳細（変更file・text/binary/unavailable diff）を読み取れるようにした。
- `git add`後のcommit失敗および初期化後のignore書込失敗は、状態が変化した可能性をアプリerrorで明示する。
- ADR 0013へM3のindex、preview、初期化安全方針を記録した。

## 11:41 — Macリリース版でM3のnative smoke testを実施

### 指示

- Macのロック解除後、Macを優先してテストを進める
- Windowsのテストは後回しにする

### 対応

- 一時Unity project `/private/tmp/vsedi-macos-smoke.Ig5tpz` を作成し、Unity 2022.3.22f1、`Assets`、`Packages/manifest.json`、`ProjectSettings/ProjectVersion.txt` を用意した
- Macのリリース版でUnity project検出、System Git診断、初期化preview、`.gitignore` / `Packages/.gitignore`生成を確認した
- 初回保存で4ファイルを保存し、次の保存で `Assets/hello.txt` を追加した
- 保存履歴、commit詳細、text diff、未管理ファイルの表示制御を確認した
- StrictMode下の重複refreshを防ぐguardと、repository未検出時の初期化カード表示条件を修正した
- 保存説明文をM3実装後の内容へ更新した

### 結果

- 初回保存 `86edf16`、2回目保存 `15ac83f` を確認した
- `pnpm typecheck`、Rust test 27件、Clippy、`git diff --check` が成功した
- Mac bundle `/Users/potapoyo/git/Vsedi/src-tauri/target/release/bundle/macos/Vsedi.app` とDMGの再生成に成功した
- Windowsのテストは未実施とし、後続課題へ残した

## 12:04 — Macテスト後のUI修正をコミット

### 指示

- この状態で一度コミットする

### 結果

- commit `0b1f791`（`Harden native refresh and repository initialization UI`）を作成した
- Codex identityと `Codex-PC: Yuks-Mac-mini` trailerを付与した
- コミット後のワークツリーはクリーンだった

## 12:06 — 日記の記載状況を確認

### 指示

- M3作業とMacテストが日記に記載されているか確認する

### 結果

- M3計画・実装に加え、Mac native smoke test、Windows延期、コミット結果まで本ファイルへ追記した

## 12:25 — Mac残工程を継続確認

### 指示

- Macのロック解除後、残っているMac側の確認を続ける

### 対応

- 「ログフォルダ」からFinderを開き、`/Users/potapoyo/Library/Logs/com.vsedi.desktop` 配下のログファイルを確認した
- 親repository `/private/tmp/vsedi-macos-parent-smoke.9gYnsD` とUnity project `UnityChild` を用意し、project外の `shared/outside.txt` が「project外」として表示されることを確認した
- `GIT_ROOT_OUTSIDE_PROJECT` の診断が表示され、repository全体を保存対象として扱うことを確認した
- 修正後のMac `.app` を再起動し、保存説明文が実装済みの内容へ更新されていることを確認した
- Finderから `Vsedi_0.1.0_aarch64.dmg` をマウントし、`/Volumes/Vsedi/Vsedi.app` を起動して同じ診断画面を確認した。確認後、DMGを取り外した

### 結果

- Macのログフォルダ、親repository境界、DMG同梱アプリの起動確認が完了した
- Windows native起動・installer確認は引き続き未実施とした

## 12:33 — Windowsを保留し、M3の保存安全性を補強

### 指示

- Windowsの作業は後に回し、その他の工程を進める

### 対応

- 追加した安全テストで、同じpathのファイル内容だけが保存preview後に変化した場合、従来のstatus tokenでは検出できないことを確認した
- 変更ファイルの内容をストリーム読み取りするfingerprintをstatus tokenへ追加し、保存直前の再検証で内容変更も停止できるようにした
- 既存staged変更を検出してHEADを変更せず停止するテストを追加した
- 初期化preview後のignore変更を検出してrepositoryを作成しないテスト、既存repositoryで重複初期化を拒否するテストを追加した
- ADR 0013へ、status出力と変更内容fingerprintを使うTOCTOU対策を追記した

### 結果

- Rustテストは31件すべて成功した
- Clippy、TypeScript typecheck、生成型チェック、Vite production buildが成功した
- 修正後のMac `.app` と `Vsedi_0.1.0_aarch64.dmg` を再生成し、`.app` の起動と診断画面を確認した
- Windows native起動・installer確認は保留したままとした

## 17:09 — ログレベル変更と全ログ表示を実装

### 指示

- ログに表示される内容を増やし、ログレベルを変更できるようにする
- いったん保持期間内の全ログを表示する

### 対応

- `ERROR`、`WARN`、`INFO`、`DEBUG`、`TRACE` の5段階を設定画面から選択できるようにした
- 選択したログレベルは実行中に即時反映し、`settings.json`へ保存して再起動後も適用するようにした
- ログウィンドウの500行制限を外し、保持期間内の全ログを読み込んで表示するようにした
- project検査、repository/worktree、保存、初期化、履歴、diff、外部processへ構造化ログを追加した。外部processの引数や標準出力などの機微情報は記録しない
- READMEとアプリケーションログADRへ設定方法と表示仕様を追記した

### 結果

- Rustテスト32件、Clippy、TypeScript typecheck、型生成の連続生成確認、Vite production build、`git diff --check` が成功した
- Mac release版でログ設定の5項目を確認し、`TRACE`への変更が即時反映されることを確認した
- ログウィンドウで保持期間内の全ログを表示し、97行のログを確認した
- Windows native起動・installer確認は引き続き保留した

## 画面分割と設定スコープの設計を開始

### 指示

- ホームで対象 repository を選択し、その後に repository 操作画面へ移る構成を設計する
- 保存履歴を操作する画面、全体設定、repository固有設定を分離する
- 実装前に設計から始める

### 対応

- `docs/design/application-navigation-and-settings.md` を追加した
- ホーム、現在の作業、保存履歴、リポジトリ設定、全体設定からなる情報構造と画面遷移を定義した
- VPM追跡方針を「全体の既定値」と「repository固有override」に分け、実効値の解決順を定義した
- repository固有設定はapp dataの `settings.json` に保持し、設定変更だけでrepositoryをdirtyにしない方針とした
- 既存機能の移動先、Frontendの状態境界、段階的な実装順序、受け入れ条件を整理した

### 結果

- Phase 1ではbackendや設定schemaを変えず、画面分割とnavigationを先に行う設計とした
- Phase 2で設定schema migrationとrepository overrideを追加する

## 17:35 — 画面分割のPhase 1を実装

### 指示

- 設計に基づいて画面構成を実装する

### 対応

- 起動時に最近使ったprojectを自動で開かず、対象選択専用のホームを表示するようにした
- ホームからprojectを選択すると、repository単位の「現在の作業」画面へ移るnavigationを追加した
- 既存の変更確認、保存メモ、作業保存、初期化previewを「現在の作業」画面へ移した
- commit一覧、commit詳細、file diffを「保存履歴」画面へ分離した
- repositoryごとの診断・VPM既定値の由来・project情報を「リポジトリ設定」画面へ移した
- 実行環境、新規repositoryの既定値、ログと診断を「全体設定」へ移した
- repository切替中に古い読み込み結果が新しい画面へ反映されないよう、選択世代を確認する処理を追加した

### 結果

- TypeScript typecheck、Vite production build、Rustテスト32件、Clippy、`git diff --check`が成功した
- Mac release版でホーム、現在の作業、保存履歴、リポジトリ設定、全体設定の遷移と表示を確認した
- repository固有のVPM overrideと設定schema migrationは、設計どおり次のPhase 2へ残した

## 管理Project一覧とカテゴリ分類を実装

### 指示

- 「最近のProject」を廃止し、管理しているProjectの一覧を作る
- 最終更新が新しいProjectを一番上に表示する
- アプリ内でProjectごとのカテゴリを設定できるようにする

### 対応

- Project登録の10件上限を外し、登録済みProjectをすべて管理一覧へ表示するようにした
- Projectを開いた時とカテゴリを変更した時に管理上の最終更新日時を更新し、新しい順に表示するようにした
- Projectごとに自由入力のカテゴリを設定・解除できるUIを追加した
- カテゴリによる管理一覧の絞り込みを追加した
- categoryはアプリの `settings.json` に保存し、repository内を変更しないようにした
- settings schemaを4へ更新し、schema 1〜3のProjectへcategoryを補うmigrationを追加した

### 結果

- Rustテスト34件、Clippy、TypeScript typecheck、Vite production build、`git diff --check`が成功した
- 既存Projectはmigration後もカテゴリ未設定のまま保持され、ユーザーがアプリから分類できる

## 20:39 — CI中断記録を読み込み、M3を再計画

### 指示

- CIの中断後に更新された日記を読み込む
- 中断内容を踏まえてM3の計画を作り直す

### 確認

- `origin/main`をcommit `6b5f397`までfetchし、CI引き継ぎの日記を確認した
- Windows native UI workflow run `31591172469`では、Node.js 22、pnpm、frontend build、native testの型チェック、Tauri release build、WebView2 / Edge WebDriverの準備まで成功していた
- `POST /session`で`DevToolsActivePort file doesn't exist`となり、native test本体は0件のまま終了していた
- release executableをcapabilityとserviceの双方へ明示したPR #22後も再現したため、native UI CIの調査は一旦中断されている
- この中断は通常CIやM3製品ロジックの失敗ではない一方、Windows native確認が完了した証拠にもならないと整理した

### 再計画

- `docs/development/m3-plan.md`を現在の実装状況に合わせて全面更新した
- M3を、製品実装、通常CI、Playwright smoke、native UI test、配布物smokeの独立した検証層に分けた
- まず未コミットの管理Project・カテゴリ実装を確定し、repository固有設定と現行UI用smoke testを完成させる順序とした
- Windows native WebDriver問題は製品実装を止めない独立工程へ移し、安定するまでは手動workflowのままとした
- Internal Alphaでは両OSの配布物smokeを引き続き必須とし、native UI CIが未安定な場合も未確認項目を成功扱いしない方針を明記した

### 次の作業

- Phase Aとして管理Project・カテゴリ差分をreviewし、通常CI相当の検証後に単独commitへまとめる
- その後、`origin/main`のUI test基盤を取り込み、画面分割後のテストケースへ更新する

## 21:00 — リポジトリ固有VPM設定を実装

### 指示

- CI以外のM3計画にある実装を進める
- Macがロックされていて実機テストできない場合は、他の作業を先に進める
- 作業単位ごとに日記を更新してコミットする

### 対応

- `AppSettings`へ`repositorySettings`を追加し、schemaを4から5へ更新した
- schema 1〜4からのmigrationで、既存設定を壊さずrepository設定配列を追加するようにした
- repository rootをcanonicalizeしてVPM tracking policyのoverrideを解決するRust側の正本処理を追加した
- project診断、repository初期化preview、初期化実行が同じ実効VPM方針をsettingsから解決するようにした
- repository設定画面に「全体設定に従う／除外する／含める」、実効値、設定の由来を追加した
- repository設定は`settings.json`だけに保存し、repository内のファイルを変更しない方針を維持した
- architecture、画面設計、requirements、M3計画をschema 5とPhase 2の進捗へ更新した

### 検証

- Rustテスト37件、Clippy、TypeScript typecheck、Vite production build、cargo fmt、`git diff --check`が成功した
- 端末がロック中のため、macOSのnative UI確認は実施していない
- WindowsおよびCIの作業は今回の変更対象から除外した

## 21:08 — ignore template編集と既存repositoryへの差分適用を実装

### 対応

- 全体設定にUnity / VPM ignore templateのtextarea editorを追加し、空行・コメントを保持して`settings.json`へ保存できるようにした
- `preview_ignore_rules`で既存repositoryの`.gitignore`と`Packages/.gitignore`をtemplateと比較し、不足ruleだけを表示するようにした
- `apply_ignore_rules`でpreview tokenを再検証してから不足ruleだけを追加するようにした
- 既存ruleの削除・置換は行わず、改行形式を保持する安全条件を維持した
- stale preview、既存内容保持、CRLF、VPM exclude ruleの追加をRustテストで確認した
- 新しいTauri command、共有型、エラーコード、M3計画・requirementsを更新した

### 検証

- Rustテスト39件、Clippy、TypeScript typecheck、Vite production build、生成型チェック、cargo fmt、`git diff --check`が成功した
- Mac端末がロック中のため、native UIでのtemplate編集・preview・適用確認は未実施

## 21:15 — 管理Projectのstale path操作を実装

### 対応

- 存在しなくなったProjectに「場所を再指定」を追加し、選び直したProjectへカテゴリと最終更新日時を引き継ぐようにした
- 管理一覧からProjectを削除できるようにした。削除時はrepository内のファイルを変更せず、settings.jsonの登録だけを更新する
- 再指定時に同じpathを重複登録しないよう、既存pathと置換元pathをまとめて除外する処理を追加した
- Mac実機確認は端末ロック中のため行わず、UI typecheck、Vite production build、差分チェックを実施した

## 21:22 — M3のローカル統合フローを追加検証

### 対応

- 一時repositoryで初期化preview→repository初期化→変更一覧→作業保存→履歴→commit詳細までを通すRust統合テストを追加した
- 初期化で生成されるignore ruleと保存対象の変更を同じflowで確認し、保存後にworktreeが空になることを検証した
- settingsのテストマトリクスをschema 1〜5へ更新した

### 検証

- Mac端末は引き続きロック中のため、native UI確認は行っていない

## 21:14 — Macロック中の非CI工程へ切り替え

### 方針

- Macのロック中はnative UI・配布物の実機確認を行わず、製品コード、Rust統合テスト、設定・ignore安全性の作業を継続する
- Playwright workflowとnative UI CIは今回の作業列から外し、ローカル回帰検証と手動配布物確認を先に進める

### 更新

- M3計画に「CIを除外した実行順序」を追加し、Phase C / Phase Eを保留工程として明示した
- READMEの現在段階とignore templateの操作方法を、実装済みの設定画面・差分previewへ更新した

## 21:25 — ホームの読み込み状態を補強

### 対応

- settings読込中に「まだProjectは登録されていません」と誤表示しないよう、管理Project一覧へ読み込み中表示を追加した
- repository固有VPM方針の要件をrequirementsへ明記した
- Mac実機確認は端末ロック中のため行っていない

## タグ機能への移行を開始

### 指示

- Projectの単一カテゴリを複数設定できるタグへ変更する
- 管理Projectの画面で検索できるようにする
- 先に立てたUI修正計画へ組み込んで実装する

### 対応

- `RecentProject.category` を `RecentProject.tags` へ変更し、現行settings schemaを6へ更新した
- schema 4〜5の既存カテゴリを1件のタグへ変換し、schema 1〜3のProjectには空のタグ配列を補うmigrationを追加した
- 設定保存時にタグの前後空白、空文字、重複を正規化するようにした
- TypeScript bindingをRustの正本型から再生成した

### 検証

- Rust test 41件が成功した
- TypeScript typecheckが成功した
- ホームUIとリポジトリ設定UI、ドキュメント更新は次の作業単位で続ける

## タグ編集・Project検索UIを実装

### 対応

- ホームのProjectカードから単一カテゴリ編集欄を削除し、リポジトリ設定へ開く歯車ボタンへ変更した
- リポジトリ設定に複数タグの追加・削除・全解除・保存を追加した
- タグはEnter、半角カンマ、読点で分割し、空文字・重複を除去して保存するようにした
- ホームの管理Project一覧へProject名・path・タグの検索欄を追加した
- タグチップによるOR絞り込みと、検索・タグ絞り込みの組み合わせに対応した
- 既存の最終更新順、stale Projectの再指定、一覧からの削除を維持した
- requirements、architecture、画面設計、M3計画、READMEをカテゴリからタグの仕様へ更新した

### 検証

- Rust test 42件が成功した
- TypeScript typecheck、Vite production build、Clippy、生成型チェック、差分チェックが成功した
- Mac native UIの再確認は次の実機確認工程で行う

## ホームProject一覧の表示操作を改善

### 指示

- 上部の案内帯を最小化し、1行表示と元の表示を切り替えられるようにする
- 「管理しているProject」の横の虫眼鏡で検索欄を表示・非表示にする
- Avatarは人物、Worldは地球儀のアイコンをProject名の前に表示し、マウスオーバーで種別を示す

### 対応

- 上部案内帯に展開・最小化ボタンを追加し、最小化時はProject追加導線を残した1行表示にした
- Project一覧の検索欄とタグ絞り込みを初期状態では隠し、虫眼鏡ボタンで切り替えるようにした
- settings読込時に既存Projectを診断し、`RecentProjectStatus.projectKind`としてAvatar / Worldの種別をホームへ渡すようにした
- Projectカードのfolder名の前に人物・地球儀・folderアイコンを表示し、`title`と`aria-label`で種別を確認できるようにした

### 検証

- Rust test 42件、TypeScript typecheck、Vite production build、Clippy、生成型チェック、差分チェックが成功した
- native UIの視覚確認はMac実機確認工程で行う

## Project追加導線をホームヘッダーへ移動

### 指示

- 青帯内のProject追加ボタンを、画像の位置に合わせて移動する
- 青帯は今後のお知らせ欄として活用する

### 対応

- ホームヘッダー右側に「Projectを追加」ボタンを配置した
- 青帯からProject追加ボタンを外し、案内文と折りたたみ操作だけを残した
- 青帯の展開・最小化ラベルを「お知らせ」として扱えるようにした
- 設計書へ、ホームヘッダーとお知らせ領域の責務を追記した

### 検証

- TypeScript typecheckが成功した
- Vite production build、Rust test、Clippyは続けて実行して確認する

## Project検索欄に入力文字クリア操作を追加

### 指示

- 検索欄へ入力中の文字を削除するボタンを配置する

### 対応

- 検索入力の右端に、入力文字がある場合だけクリアボタンを表示するようにした
- クリア操作は検索文字列だけを空にし、選択中のタグ絞り込みは維持する
- ボタンに`aria-label`とツールチップを設定した

### 検証

- Rust test 42件、TypeScript typecheck、Vite production build、Clippy、生成型チェック、差分チェックが成功した

## 1行入力のクリア操作とタグ未保存状態の表示を追加

### 指示

- Project設定のタグ入力、現在の作業の保存メモなど、1行入力の右側から入力中の文字を削除できるようにする
- 「すべて解除」を「タグ全消去」に変更し、タグ編集の保存操作と同じ行へ配置する
- タグの未保存変更がユーザーに分かるよう、保存ボタンの状態を表示する

### 対応

- Projectタグ入力と現在の作業の保存メモ入力に、入力中だけ表示する×ボタンを追加した
- 「タグ全消去」へ文言を変更し、タグ保存ボタンの隣に配置した
- タグ編集前後を比較し、未保存変更がある場合は保存ボタンを赤色・有効状態にし、保存済みの場合は通常色・無効状態にした
- 共通Buttonに危険操作向けの赤色`danger`バリアントを追加した

### 検証

- Rust test 42件、TypeScript typecheck、Vite production build、Clippy、生成型チェック、差分チェックが成功した
