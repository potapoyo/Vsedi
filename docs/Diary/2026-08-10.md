# 2026-08-10

## 今日行ったこと

Vsedi の実装開始前に、製品・安全性・開発基盤に関する設計を整理し、GitHub 上の ADR / Issue / PR へ反映した。

### 製品・基盤方針

- Vsedi を「VRChat Unity プロジェクトの安全なセーブ・履歴・復元ツール」として整理した
- Tauri v2 をデスクトップ基盤として採用した
- Windows と Apple Silicon macOS を正式対応対象とした
- 初期配布は未署名とし、Windows installer / macOS DMG を最終的な配布物とする方針を整理した
- Frontend を React + TypeScript + Vite + pnpm + Tailwind CSS + shadcn/ui とした
- Rust を共有型の正本とし、`serde + ts-rs` で TypeScript 型を生成する方針を確定した

### 設定・復旧

- アプリ内部設定と持ち運び可能な環境バックアップを分離した
- 内部設定は `settings.json`、整数の `schemaVersion` を持つ方針とした
- 環境バックアップは `vsedi-environment.vsedi.json`、整数の `formatVersion` を持つ方針とした
- 設定破損時は、元ファイルを黙って上書きせず「保全 → migration → 最後に再生成」の順で扱う方針を確定した
- PC 故障時には、理解しているユーザーが `settings.json` を所定位置へ手動配置して低レベル復旧できる余地を残した

### Git / Git LFS

- Git は system Git CLI を Rust 側から利用する方針を維持した
- Git LFS は `git-lfs` executable を独立探索せず、Vsedi が利用する Git に対する `git lfs version` で診断する方針を確定した
- Git / Git LFS の未導入は可能な限りエラーではなく通常の診断状態として返すことにした

### エラー処理

- 共通 `AppError` / `ErrorCode` モデルを確定した
- Frontend は Git stderr / OS error の文字列解析で UI 分岐しない方針とした
- mutation の途中失敗では `mayHaveMutated` を使って状態変更可能性を表現することにした

### ログ

- application logging の方針を確定した
- 日次ローテーションとした
- ログ保持期間を **30日** とした
- 通常は `INFO` 以上、`DEBUG` は明示的に有効化する方針とした
- credential / token / password 等をログへ記録しないことを明記した
- remote URL は sanitize し、診断ログ export 時には project path 等の個人環境情報を匿名化する方針とした

### Rust 構成と生成型

Rust 側は次を基本 module とする方針を確定した。

```text
commands/
services/
git/
platform/
models/
errors/
logging/
settings/
```

- Tauri command は薄く保つ
- application use case は service layer に置く
- Git / OS / settings の実処理はそれぞれの module へ分離する
- Unity / VRChat 固有 module は必要になる milestone で追加する
- `ts-rs` の生成済み TypeScript 型は Git 管理する
- CI で型を再生成し、差分がないことを確認する方針とした

## Pull Request

- PR #1「Vsedi の製品定義と安全設計を追加」: マージ済み
- PR #3「対応OSとバイナリ配布を必須要件として固定」: マージ済み
- PR #5「設定復旧・Git LFS診断・エラーモデルを確定」: マージ済み
- PR #6「M1のログ仕様・Rust構成・生成型管理方針を確定」: 作成

## Issue

既存:

- #2 — M1: Tauri デスクトップアプリの基盤を構築する
- #4 — M6: 環境バックアップと復元モードを実装する

一度追加した Issue:

- #7 — M2: Unity / VRChat プロジェクト検出を実装する
- #8 — M3: ローカル保存と履歴確認を実装する
- #9 — M4: 安全な復元を実装する
- #10 — M5: リモートバックアップと安全な同期を実装する

## 17:09 — 日記運用とIssue運用の変更

### 指示

- 日記は日末のまとめではなく、Vsedi に関する指示ごとに追記し、経緯が分かるログとして残す
- 05:00 を1日の区切りとし、1日1ファイルにする
- Issue は先のマイルストーンを事前作成せず、M1 完了後に M2 を作るように順次作成する
- M1 以外の現在のマイルストーン Issue は削除する方針へ変更する

### 対応

- `docs/Diary/README.md` を更新し、05:00〜翌04:59を1日として扱うルールを追加した
- 今後はユーザーのVsedi関連指示ごとに、その日のファイルへ時系列で追記する運用とした
- 方針変更や前言撤回も過去記録を消さず、新しいログエントリとして残すことにした
- GitHub connector では Issue の完全削除操作が提供されていないため、M1以外のマイルストーン Issue を `not planned` としてクローズした

### 結果

- #2 — M1: オープンのまま維持
- #4 — M6: `not planned` でクローズ
- #7 — M2: `not planned` でクローズ
- #8 — M3: `not planned` でクローズ
- #9 — M4: `not planned` でクローズ
- #10 — M5: `not planned` でクローズ
- 今後は M1 完了時に次の M2 Issue を新規作成し、その後も同様に1マイルストーンずつ進める

## 17:18 — CodexコミットのGit identity運用を追加

### 指示

- Codexが行うコミットだけ、GitのAuthor / Committerを `Codex` として識別できる運用を設計し、リポジトリの `AGENTS.md` にCodex専用ルールを追加する
- 通常のユーザーコミットには影響させず、repo-local / global Git configを恒久変更しない
- GitHub上の操作ユーザー（認証主体）と、Git commitのAuthor / Committer表示の違いを明記する
- 既存の05:00区切り・1日1ファイル・Vsedi関連の指示ごとの追記というDiary運用に従う

### 対応

- Codex用identityを `Codex <codex@users.noreply.github.com>` と定めた
- Codexがコミットを作成するときだけ `git -c user.name='Codex' -c user.email='codex@users.noreply.github.com'` を付ける方式を採用した
- 恒久的なGit config、alias、hook、環境変数への登録を禁止し、ユーザーの既存コミットを改変しないルールを `AGENTS.md` に追加した
- GitのAuthor / Committerはコミットのメタデータ、GitHubのpush・PR・Issue・コメントの操作ユーザーは認証中のGitHubアカウントであることを `AGENTS.md` に明記した

### 結果

- ルートに `AGENTS.md` を追加し、Codexだけに適用する運用をリポジトリ規約として記録した
- このDiary追記と `AGENTS.md` の追加を、上記の一時identityを使ったコミットとして検証する

## 17:21 — コミット前の最新リモート確認を追加

### 指示

- 作業を進める前に、最新のリモートリポジトリの状態を確認することを忘れない

### 対応

- `origin` を再取得し、作業ブランチの基点と `origin/main` を比較した
- `origin/main` は `b37e13a556acde49e7d611ee105ddc08bbbeffab` で、作業ブランチの基点と一致していることを確認した
- リモート取得後の差分は、意図した `AGENTS.md` の追加と本Diaryの追記だけであることを確認した

### 結果

- このコミットを作成する前のリモート確認は完了した
- 今後もCodexがVsediで変更・コミットを行う前に、リモートの最新状態を再取得して確認する

## 17:38 — Issue #2 の M1 基盤実装を開始

### 指示

- 最新を pull してから Issue:M1 の実装を進める

### 対応

- `origin/main` を fast-forward pull し、`95400f9` を作業基点にした
- Issue #2 本文と設計確定コメントを確認した
- `src-tauri` に Tauri v2 の Rust backend と `commands / services / git / platform / models / errors / logging / settings` 構成を追加した
- React + TypeScript + Vite + pnpm、Tailwind CSS、shadcn/ui 形式の最小 UI と native directory picker を追加した
- Git / Git LFS 診断、Unity project 診断、構造化 `AppError`、設定保全、日次ログ、診断ログ export、`ts-rs` 型生成手順を実装した

### 結果

- 作業ツリーは実装途中の未コミット状態
- ローカルに Rust / Cargo がないため、Rust compile / native build は未検証
- TypeScript 依存を導入し、`pnpm typecheck` と `pnpm build` が成功した
- Tauri desktop icon と最小限の CI（Frontend build / Rust test / 型生成差分検証）を追加した
- Rust の公式 API 仕様を照合し、`ts-rs` の明示生成と Tauri Store の runtime API に合わせた

## 18:07 — Rust環境の導入とRust build確認

### 指示

- このPCに Rust の環境を入れ、Rust の build を確認する

### 対応

- Homebrew の `rustup` を導入し、stable toolchain（`aarch64-apple-darwin`）を初期化した
- `~/.zshrc` に `/opt/homebrew/opt/rustup/bin` を PATH へ追加し、新しいシェルでも `rustc` / `cargo` を利用できるようにした
- `ts-rs` の単一ファイル型生成で不要な import を除去する処理を追加し、frontend の型検査を修正した

### 結果

- `rustc 1.97.1` / `cargo 1.97.1` を確認した
- `cargo fmt --check` 成功
- `cargo test --manifest-path src-tauri/Cargo.toml` 成功（11 passed, 0 failed）
- `cargo build --manifest-path src-tauri/Cargo.toml` 成功
- `pnpm typecheck` / `pnpm build` 成功
- 作業ツリーは引き続き未コミット状態

## 20:16 — GUIテスト用の開発環境を導入

### 指示

- このマシンでVsediのGUIテストができる環境を用意する
- Rust / Node.js のバージョンを開発環境ごとに変更しやすくする
- 人間が確認できるGUIの確認方法を記録する

### 対応

- 既存の `nvm` は他のプロジェクトのために残し、VsediではHomebrewの `mise` を優先するよう `~/.zshrc` を更新した
- リポジトリに `.mise.toml` を追加し、Node.js `22.23.1`、Rust `1.97.1`（minimal profile）をプロジェクト単位で固定した
- Corepackで `pnpm 11.16.0` を有効化し、`pnpm install --frozen-lockfile` で依存関係を導入した
- READMEに、GUI起動、環境診断、再診断、Unity project選択、終了までの目視スモークテスト手順を追加した
- 最初の `pnpm tauri dev` で、`export_types` とアプリ本体の2バイナリがあり実行対象を選べない問題を確認した。`src-tauri/Cargo.toml` に `default-run = "vsedi"` を追加して修正した

### 結果

- `mise exec -- pnpm build` 成功
- `mise exec -- cargo test --manifest-path src-tauri/Cargo.toml` 成功（11 passed, 0 failed）
- `mise exec -- pnpm check-generated-types` 成功
- 修正後の `pnpm tauri dev` は `target/debug/vsedi` を起動し、ユーザーがVsediのネイティブGUIウィンドウ表示を目視確認した
- 自動E2Eフレームワークはまだ導入せず、現段階ではREADMEに手動スモークテストの確認方法を記録した

## 20:19 — 最終検証

### 対応

- 開発用Rust toolchainに `rustfmt` と `clippy` を追加した
- `cargo clippy --all-targets -- -D warnings` で検出された既存コードの2件のlintを、挙動を変えない形で修正した

### 結果

- `cargo fmt --check` 成功
- `cargo clippy --all-targets -- -D warnings` 成功
- `cargo test --manifest-path src-tauri/Cargo.toml` 成功（11 passed, 0 failed）
- 先に実施した `pnpm build`、型生成差分チェック、ユーザーによるGUI目視確認と合わせ、今回の開発環境導入とGUI起動確認を完了した
